.vercel
.env*

# Vite builds to public/, not dist/. A stray dist/ silently became the
# Capacitor webDir target once already (see commit message for details).
dist/

# Xcode drops per-machine SwiftPM state here; not project config.
ios/App/App.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/
