{
  "framework": null,
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/finance-tracker run build",
  "outputDirectory": "artifacts/finance-tracker/dist/public",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
